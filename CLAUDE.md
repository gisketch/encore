@AGENTS.md

## Claude Code Notes

- Sonata skills live in `.claude/skills/` (mirrored from `.agents/skills/`). Invoke them as skills, e.g. `/sonata-work`.
- `$sonata-<name>` references inside skill bodies refer to the skill of the same name.
