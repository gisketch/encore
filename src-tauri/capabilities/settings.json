{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window: folder-picker dialog, titlebar drag, and close (converted to hide by the window-event handler).",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
