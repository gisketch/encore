{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the Encore main window: read defaults plus the self-management the floating bar needs (drag region, expand/collapse resize, and hiding itself to the menu bar).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-hide"
  ]
}
