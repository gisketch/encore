{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panels",
  "description": "Library and Editor windows: titlebar drag, close (converted to hide), and hide for the editor's back-to-library link.",
  "windows": ["library", "editor"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-start-dragging"
  ]
}
