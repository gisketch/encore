[package]
name = "encore"
version = "0.1.0"
description = "A local replay buffer for reproducible QA evidence"
authors = ["Ghegi Jimenez"]
edition = "2021"

[lib]
name = "encore_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
